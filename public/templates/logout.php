<!-- Форма выхода, если пользователь авторизирован -->
<div class="center">
    <p>{{lang.message_auth}}</p>
    <a href="{{uriPage.home}}">{{lang.page_profile}}</a> 
    <p>
        <a href="{{uriPage.logout}}" class="btn btn-danger">{{lang.logout}}</a>
    </p>
</div>

